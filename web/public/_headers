/*
  Referrer-Policy: strict-origin-when-cross-origin
  X-Content-Type-Options: nosniff
  Permissions-Policy: camera=(), microphone=(), geolocation=()

# CSP is intentionally set by functions/_middleware.js, not this static file.
# Production HTML receives a per-request nonce-based policy.

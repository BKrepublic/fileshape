/*
  Referrer-Policy: strict-origin-when-cross-origin
  X-Content-Type-Options: nosniff
  Permissions-Policy: camera=(), microphone=(), geolocation=()

# Content-Security-Policy is intentionally not set here yet.
# FileShape currently carries a restrictive CSP in its HTML. Before AdSense is
# enabled, move CSP enforcement to the response layer and use Google's supported
# nonce-based strict CSP so ad-serving domains do not need a brittle allowlist.
